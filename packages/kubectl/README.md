# kubectl

This package builds `kubectl` from the upstream Kubernetes source tree.

Notes:

- Uses the upstream `kubernetes` source archive
- Follows the Arch PKGBUILD build flags closely for the `kubectl` build only
- Installs bash, zsh, and fish completions generated by the built binary
