_sops()
{
    local cur prev words cword
    _init_completion || return

    COMPREPLY=($(
        COMP_WORDS="${COMP_WORDS[*]}" \
        COMP_CWORD="${COMP_CWORD}" \
        "${words[0]}" --generate-bash-completion 2>/dev/null
    ))
}

complete -F _sops sops
