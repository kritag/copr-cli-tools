# sops

This package builds `sops` from the upstream source release.

Notes:

- Installs the `sops` binary
- Installs bash and zsh completion files maintained in this packaging repo
- Uses a release tarball instead of a signed git checkout for COPR compatibility
