# stern

This package builds `stern` from the upstream source release.

Notes:

- Installs the `stern` binary
- Generates and installs bash, fish, and zsh completions
- Installs upstream Markdown documentation
