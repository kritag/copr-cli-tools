# krew

This package builds the `krew` kubectl plugin manager from source.

Notes:

- Installs the `kubectl-krew` plugin binary
- Installs the `validate-krew-manifest` helper binary
- Requires `kubectl` and `git-core` at runtime
