# kubectx

This package builds `kubectx` from the upstream source release.

Notes:

- Installs both `kubectx` and `kubens`
- Installs bash, zsh, and fish completions for both commands
- Requires `kubectl` at runtime
- `fzf` is optional if you want interactive selection
