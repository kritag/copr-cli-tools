OWNER=obsidianmd
REPO=obsidian-releases
# Upstream sometimes ships a release with only the Android APK (e.g. v1.13.8).
# Track the newest release that actually carries the amd64 .deb this spec needs.
UPSTREAM_RELEASE_ASSET_PATTERN="^obsidian_.*_amd64\.deb$"
